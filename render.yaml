services:
  - type: web
    name: namagawa-delivery-allocation
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
    disk:
      name: delivery-records
      mountPath: /var/data
      sizeGB: 1